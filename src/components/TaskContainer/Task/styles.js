import { makeStyles } from "@material-ui/styles";

export default makeStyles({
    taskBox: {
        display: 'flex',        
        alignItems: 'center',
    },

    taskChecked: {
        textDecoration: 'line-through',
    }
});
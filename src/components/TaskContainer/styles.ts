import { makeStyles } from "@material-ui/styles";

export default makeStyles({
    taskContainer: {
        padding: '0px 15px'
    },

    taskPaper: {
        padding: '10px',
    },

    taskEmpty: {
        textAlign: 'center',
    }
});